{
  "db": {
    "name": "db",
    "connector": "mongodb",
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "database": "${DB_NAME}",
    "user": "${DB_USERNAME}",
    "password": "${DB_PASSWORD}"
  },
  "storage": {
    "acl": "public-read"
  }
}
